# --- Bí mật: TUYỆT ĐỐI không đưa lên GitHub ---
.env
.env.*
!.env.example
*.key
secrets.json

# --- Dữ liệu thật: repo là public nên không chứa dữ liệu kho ---
*.csv
*.xlsx
*.xls
data/*.json
data/snapshots/

# --- Output kiểm thử: có ảnh chụp, dữ liệu thật và email nhân sự ---
# knowledge/BAN-GIAO.md vẫn ghi là dòng này đã có từ trước, nhưng thực tế chưa.
# Bổ sung 27/08/2026 khi bản xem trước màn hình Cấu hình (có email @havas.vn)
# suýt được đưa vào repo PUBLIC.
test/

# --- Rác hệ thống / công cụ ---
.DS_Store
.playwright-cli/
*.log
node_modules/
dist/
output/
__pycache__/
*.pyc
.venv/
venv/
